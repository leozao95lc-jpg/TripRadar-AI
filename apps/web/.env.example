# URL pública da API (apps/api) — sem barra final.
NEXT_PUBLIC_API_URL=http://localhost:8000

# URL pública deste frontend — usada em sitemap.ts, robots.ts e metadados
# (generateMetadata/OpenGraph). Sem barra final.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
